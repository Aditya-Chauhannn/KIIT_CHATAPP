<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="10dp"
    android:orientation="vertical"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:gravity="start"
    android:layout_marginBottom="10dp"
    app:cardElevation="4dp"
    app:cardBackgroundColor="@android:color/transparent"
    android:background="@drawable/bg_message_receive">

    <!-- Horizontal container for sender and roll number -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/senderNameTextView"
            android:text="Sender Name"
            android:textStyle="bold"
            android:textSize="14sp"
            android:textColor="#8A784E"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />

        <!-- Small space between name and roll -->
        <Space
            android:layout_width="8dp"
            android:layout_height="wrap_content" />

        <TextView
            android:id="@+id/rollNumberTextView"
            android:text="Roll: 12345678"
            android:textColor="#888888"
            android:textSize="13sp"
            android:layout_width="wrap_content"
            android:visibility="gone"
            android:layout_height="wrap_content" />
    </LinearLayout>

    <TextView
        android:id="@+id/receivedMessageTextView"
        android:text="Received message"
        android:layout_width="wrap_content"
        android:textStyle="bold"
        android:textSize="14sp"
        android:textColor="@color/green"
        android:layout_height="wrap_content" />

    <ImageView
        android:id="@+id/receivedImageView"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"/>

    <TextView
        android:id="@+id/receivedTimestampTextView"
        android:text="12:01 PM"
        android:textSize="10sp"
        android:textColor="@color/green"
        android:textStyle="italic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />
</LinearLayout>